{"id":"C001","category":"compiler","group":"Front end and language","order":1,"title":"Lexer over the closed token registry","score":1,"evidence":"src/lex.e"}
{"id":"C002","category":"compiler","group":"Front end and language","order":2,"title":"Parser and syntax tree","score":1,"evidence":"src/parse.e; a grouped return value may carry a binary operator (D247) -- after `ret` the tuple `ret (a, b)` and an ordinary `(expr)` are told apart by a scanner-copy lookahead for a comma at depth one, pinned by link/ret_group"}
{"id":"C003","category":"compiler","group":"Front end and language","order":3,"title":"Module graph and import resolution","score":1,"evidence":"src/graph.e"}
{"id":"C004","category":"compiler","group":"Front end and language","order":4,"title":"Order-independent name resolution","score":1,"evidence":"src/resolve.e"}
{"id":"C005","category":"compiler","group":"Front end and language","order":5,"title":"Integer arithmetic, bitwise, shifts, division","score":1,"evidence":"link/bitwise, link/shifts, link/division"}
{"id":"C006","category":"compiler","group":"Front end and language","order":6,"title":"if / while, break / continue, block scopes","score":1,"evidence":"link/control; `else if` chains lower as the nested if they are (D245), pinned by link/else_if"}
{"id":"C007","category":"compiler","group":"Front end and language","order":7,"title":"when over target.arch / target.os","score":1,"evidence":"Section 6's conditional compilation (D216): `when` takes a condition over `target.arch` and `target.os` compared with their members, under `!`, `&&`, `||` and parentheses, settled at compile time; both blocks type check and the taken one is emitted; any other condition is a bool the interpreter evaluates (D220), and one it cannot is refused under E-COMPTIME-9999 (link/when_target on both platforms, check/when_condition). `target.arch` and `target.os` are values of `target.Arch` and `target.Os` anywhere (D223): compared, switched over exhaustively, held and passed"}
{"id":"C008","category":"compiler","group":"Front end and language","order":8,"title":"for over ranges, arrays and slices","score":1,"evidence":"tests/neper0"}
{"id":"C009","category":"compiler","group":"Front end and language","order":9,"title":"Fixed arrays, zero / undef, .len","score":1,"evidence":"link/storage"}
{"id":"C010","category":"compiler","group":"Front end and language","order":10,"title":"Slicing and mutability propagation","score":1,"evidence":"link/storage"}
{"id":"C011","category":"compiler","group":"Front end and language","order":11,"title":"Structs: layout, literals, field places","score":1,"evidence":"link/aggregate"}
{"id":"C012","category":"compiler","group":"Front end and language","order":12,"title":"Pointers, & / *, *const, auto-deref","score":1,"evidence":"link/aggregate"}
{"id":"C013","category":"compiler","group":"Front end and language","order":13,"title":"Aggregate copies and ABI pass / return","score":1,"evidence":"link/advanced"}
{"id":"C014","category":"compiler","group":"Front end and language","order":14,"title":"Enums, bare unions, union enum","score":1,"evidence":"link/variants"}
{"id":"C015","category":"compiler","group":"Front end and language","order":15,"title":"switch, exhaustive and non-fallthrough","score":1,"evidence":"fixtures/variants"}
{"id":"C016","category":"compiler","group":"Front end and language","order":16,"title":"defer, both forms","score":1,"evidence":"tests/neper0"}
{"id":"C017","category":"compiler","group":"Front end and language","order":17,"title":"err / try / ok and named errors","score":1,"evidence":"link/error_collision"}
{"id":"C018","category":"compiler","group":"Front end and language","order":18,"title":"Multi-return and destructuring","score":1,"evidence":"link/scalar"}
{"id":"C019","category":"compiler","group":"Front end and language","order":19,"title":"const and integer comptime folding","score":1,"evidence":"link/generic_folding"}
{"id":"C020","category":"compiler","group":"Front end and language","order":20,"title":"Generic functions [T: type], [N: usize]","score":1,"evidence":"link/generic_instances"}
{"id":"C021","category":"compiler","group":"Front end and language","order":21,"title":"Generic aggregate types","score":1,"evidence":"link/generic_same_name; check/generic_instance_field and link/generic_instance_alias pin one used as a field type, named directly and through an alias; link/data_iter pins a nested instance as a type argument -- `Filter[Map[Iter[i64], i64, i64], i64]` -- which read its inner argument as the outer's until the argument block was claimed before it was filled (D145)"}
{"id":"C022","category":"compiler","group":"Front end and language","order":22,"title":"Protocol resolution, spec 9 rules 3 and 5","score":1,"evidence":"check/protocol_*"}
{"id":"C023","category":"compiler","group":"Front end and language","order":23,"title":"Iterator protocol <t>_next","score":1,"evidence":"tests/neper0; link/data_iter calls it through a type parameter as `I.next(&it)`, on a generic iterator whose `_next` is instantiated from the receiver's own arguments (D145)"}
{"id":"C024","category":"compiler","group":"Front end and language","order":24,"title":"Supplied cmp (rule 4)","score":1,"evidence":"link/sequence_cmp, tagged_union_cmp"}
{"id":"C025","category":"compiler","group":"Front end and language","order":25,"title":"Supplied hash (rule 4)","score":1,"evidence":"link/supplied_hash, folded_hash"}
{"id":"C026","category":"compiler","group":"Front end and language","order":26,"title":"Supplied eq (rule 4)","score":1,"evidence":"link/supplied_eq; link/test_assert pins the floats, which rule 4 always listed and the compiler had not supplied: every NaN equals every other and the two zeros are one (D142)"}
{"id":"C027","category":"compiler","group":"Front end and language","order":27,"title":"Supplied format (rule 4)","score":1,"evidence":"link/str_format, link/io_printf expand scalars, str, bool and err; slices and arrays recurse an element at a time as `[a, b]`, nesting included (D189); an enum prints its variant name (D190); a type's own `<t>_format` is called with the builder, inside a sequence too (D191); check/format_compound_argument pins the struct without one"}
{"id":"C028","category":"compiler","group":"Front end and language","order":28,"title":"Scalar floating point f32 / f64","score":1,"evidence":"link/float_scalar; f16 / bf16 still unlowered. link/math_exact pins `math.sqrt` as the one float intrinsic -- `sqrtss`/`sqrtsd` behind a NIR opcode of its own, correctly rounded and checked by its bits (D146). `fma` is exact by integer arithmetic on the significands, and the thirteen transcendentals are fdlibm's over f64 with a Payne-Hanek reduction of its own, each measured within 1 ULP against mpmath at 200 bits in the four link/math_* fixtures (D147)"}
{"id":"C030","category":"compiler","group":"Front end and language","order":30,"title":"Atomic[T] and memory orderings","score":1,"evidence":"link/atomic_ops runs all thirteen across every width and both signs; link/atomic_threads proves the lock prefix under contention; check/atomic_* pin the ordering rules. e.sync and e.channel are unblocked"}
{"id":"C031","category":"compiler","group":"Front end and language","order":31,"title":"extern with @import / @cc and the C ABI","score":1,"evidence":"link/extern_import binds and calls foreign symbols on both platforms: a PE import directory with one descriptor per library, and a dynamic ELF with PT_INTERP, DT_NEEDED and GLOB_DAT slots. It also pins a narrow signed return being widened, which the convention leaves undefined above the declared width -- an unwidened -1 read as 0xFFFFFFFF made every `< 0` check on a foreign call pass. Section 5's closed table of which types cross is enforced at every extern declaration (D192): check/extern_slice_parameter and check/extern_slice_return pin a slice and a tagged union refused. A trailing `...` is a C variadic (D193): link/extern_variadic prints through snprintf with an f64 past the fixed parameters on both conventions, and check/variadic_narrow_argument pins that an i16 there is refused rather than promoted"}
{"id":"C034","category":"compiler","group":"Front end and language","order":34,"title":"Unsafe boundaries enumerable (v1 precursor for T2 H27)","score":1.0,"evidence":"The build manifest's `unsafe` array lists every declared and trusted boundary with module, function and line (D355, D371), including every dereference inside `@nocheck` (D497); symbol indexes carry the boundary kinds (D513). Context queries now describe an intended `--unchecked` image as `checks: off` plus one whole-image boundary for every subject (D555), and query batches preserve that policy across every context and catalogue answer (D556). `manifest-em ARTIFACT... --json` completes the source-free path: it reads required Inventory sections, preserves checked/unchecked policy and hashes every artifact, with exact source-manifest parity on both hosts (D557)."}
{"id":"C037","category":"compiler","group":"Front end and language","order":37,"title":"Semantic search and safe change planning (v1 precursor for T2 H17)","score":1.0,"evidence":"`uses-file --json --symbol module.name` (D362): every use the checker resolved -- calls of the function and its instances, dispatches that chose it, instantiations, its name as a value -- with the enclosing function and span, the roots that keep it alive, the count of calls through values that no name can trace, and a completeness flag; the contract says what a safe-delete claim and a rename plan may conclude. A rename is a plan with file-hash preconditions and a re-check postcondition, applied by nothing but the harness (D376); the signature-change plan -- a parameter added last at the declaration, its argument at every resolved call, refused with the site where the function is a value or a protocol's choice -- round-tripped by both suites (D406); a field's uses -- every access and literal naming it, recorded by the checker -- and its rename plan, round-tripped by both suites (D420). `plan-change-signature-file --order` reorders and removes, a removal refused while the body names the parameter, applied and re-checked by both suites (D415, D439). an error's uses and rename (D451). A resource's moves are facts of its function's context page (D486): every site the checker consumed a local -- closed, returned, rebound, handed to an `own` parameter -- is a compiler-proved `move` fact with its span, and a folded `if` a `phase` fact (which D463 had written as a record with no kind); pinned per host by `context_moves`; a local bound to a view of another (`&x`, `x[a..b]`, `x.items`, a literal holding `&x`) is a `borrow` fact naming both (D487), the relation D393's rules hold the body to. The alias and same-spelling fixture (D509): `uses_alias` imports `deep as d` and `other`, both declaring `pick`, with a local spelled `pick` too; `uses-file` of `deep.pick` reports the two calls through `d` alone, `other.pick` its own two, and the rename plan rewrites those sites and the declaration and nothing of `other` or the local, applied and run on both hosts -- and found that a module beside a `src/main.e` operand was spelled under `operand` in every record and the manifest, now `project-src`. The comptime-only reference (D510): a call in a constant's initializer is evaluated by the interpreter and no body check sees it, so `uses-file` reported no use of a function only a constant reaches and the rename plan carried the declaration alone, leaving the call behind; the evaluation now records the call at its site, `uses_comptime` pins the use and the plan per host, applied and run. The callback fixture (D511): `uses_callback` binds a function to a local and passes it as an argument, called only through a parameter; `uses-file` reports the two `value` sites and one indirect call, the plan rewrites both and the declaration, applied and run on both hosts. The generated reference (D512): a plan's edit inside a range the operand's map marks as the generator's names its owner and where in the generator's input the text begins, and `apply-plan` refuses the plan whole, nothing applied; `plan_generated` pins the plan per host and the refusal. The index names every reference of a large module (D528): its tables are sized by the tree, where a cap of four thousand had dropped the rest of `check.e`'s references without a word, and renaming the compiler's own locals through it holds in both suites. A type's rename plan (D515): the subject names a type, every module's index is taken in memory and each reference whose target is the type gives a site, through a qualifier or bare, in an annotation or a literal, with the declaration; `plan_rename_type` pins the plan per host, applied and run; `uses-file` of a type reports the same sites as `type` uses (D516), pinned by `uses_type`; the functions spelled with the type's name -- its `cmp`, `eq`, `hash`, the protocol lookup's spelling -- are renamed with the type (D517), which the probe found the plan leaving behind: `sort.in_place` over the renamed type no longer found its `cmp`; and the rename of such a function on its own is refused, naming whose protocol it is by its spelling (D518), where the plan had been made and broke the program applied; only the protocol's spellings follow a type (D537), where a helper merely named for it had, and both suites rename `check.same` at five hundred and forty sites and the type `check.Type` at nearly four hundred by plan, apply, and build the stable stage from the result. `index-project` over the compiler and its library -- a hundred and forty-two modules, two hundred thousand records -- answers in seven seconds where it took a hundred and eighteen, its stream buffered, and the project walk that `check-project`, `test-project` and `index-project` share leaves another target's variant out, where `os.linux.e` on windows had failed the whole index (D544). A generated reference inside a dependency's map carries its generator ownership and original into every edit plan (D563)"}
{"id":"C038","category":"compiler","group":"Front end and language","order":38,"title":"GPU contracts frozen before M3 (M2.5-core H13)","score":1.0,"evidence":"Design scope closed by D664 (`m25-gpu-closure.md`): D660 freezes the typed fault ABI and H01-H07 mapping; the checked manifest keeps three CPU/Vulkan fault fixtures pending for M3 rather than reporting runtime evidence"}
{"id":"C039","category":"compiler","group":"Front end and language","order":39,"title":"Explicit CPU/GPU dependency composition (M2.5-core H21)","score":1.0,"evidence":"Design scope closed by D664: D661 freezes past-only completion tokens, cross-queue visibility, whole-buffer use/release tracking, failure/discovery state tables and twelve pending M3 fixtures. Runtime implementation remains M3"}
{"id":"C040","category":"compiler","group":"Front end and language","order":40,"title":"End-to-end GPU cost and artifact lifecycle (M2.5-core H22)","score":1.0,"evidence":"Design scope closed by D664: D662 freezes `StagingLimits`/`queue_with`, the two-clock timeline, complete cache identity, bounded wait-on-full staging and five pending M3 fixtures; batching, graphs, pinned transfer and optimized CPU execution remain deferred"}
{"id":"C041","category":"compiler","group":"Front end and language","order":41,"title":"Executable numerical and capability contracts (M2.5-core H23)","score":1.0,"evidence":"Design scope closed by D664: D663 serializes and validates 21 CPU/SPIR-V/PTX matrix rows, six launch-precondition classes and twelve pending CPU/Vulkan fixtures. Device modes and measurements remain M3/M4 evidence"}
{"id":"C046","category":"compiler","group":"Front end and language","order":46,"title":"Compiler responsiveness and correctness (v1 precursor for T2 H10)","score":1.0,"evidence":"Phases separated so a check links and generates nothing (D314, D320); artifacts keyed by source, mode and interface hashes and published atomically (D205-D214, D343, D369); mutation fuzzing and a nesting bound (D341); deterministic failure codes for every exhausted limit (D344); images byte-equal across worker counts and perturbed schedules (D331); stage-2 == stage-3 and clean == incremental in both suites; one-shot query latency measured -- 25 ms for a small check, 269 ms for the whole compiler -- and no daemon adopted on it (D372), a batch of queries over one check instead (D409: twenty queries 447 ms, not 6.6 s); the crew's workers set themselves up on their own threads, the body phase of the compiler's cold release build 134 -> 69 ms at eight workers (D402). `test-impact-file --changed m1,m2` names the tests an edit reaches over the resolved call graph, pinned by the corpus over the test project (D423), and `test-file --only n1,n2` runs those alone (D424). `benchmarks/metamorphic/metamorphic.py` checks two transformations that must change nothing -- comments removed through the token stream build a byte-identical image in both modes, declarations reversed behave the same -- over three fixtures in both suites (D438), and (D477) two more over four: every local renamed to a name of the same length through the index's `local` and `parameter` symbols and their references (D484; the token scanner of D477 is gone) builds a byte-identical image in both modes, and every struct's fields reversed behaves the same; every function and type renamed through the index's spans (D478) builds the same image once the names the trap messages carry are put back. `benchmarks/differential/differential.py` runs the library's SHA-256, SHA3-256 and base64 over random inputs and holds every answer to Python's hashlib and base64, both suites (D449). `benchmarks/differential/bootstrap.py` builds seventeen neper0 programs with the C bootstrap and with the compiler and holds their exits and output alike, both suites (D456). A sixth transformation (D508): every typed integer literal outside a `const` or a `case` label becomes a module-scope `const` named to the literal's length, and the image is byte-identical in both modes over the four fixtures -- a constant folds into its uses. A seventh, `formatted` (D525): the root through `neper fmt -`, idempotent and the same behaviour; and both suites format every module of the compiler, build a compiler from the result and require the compiler it builds from the original sources to be the stable stage byte for byte -- the formatter's contract over sixty-five thousand lines; and the compiler built from its own sources with every comment blanked to spaces is the stable stage byte for byte (D526), `benchmarks/metamorphic/strip_comments.py` blanking them through the token stream, and so is the compiler built with every typed literal hoisted to a constant (D527, `hoist_constants.py`), which found the proofs reading literals alone, and with every local renamed through the index (D528, `rename_locals.py`), which found the index dropping every reference past four thousand in a module; and the compiler built with every function and type renamed through the index of every module (D529, `rename_symbols.py`) builds the original sources to the stable stage byte for byte, as does the compiler built with every struct's fields reversed (D530, `reverse_fields.py`), twelve hundred fields moved, and the one built with every module's declarations in reverse order (D531, `reorder_declarations.py`) -- the seven transformations of the harness, all but the two that hold an image byte-identical by construction, turned on the compiler itself; and the library too (D532): the compiler built against `lib/` with every comment blanked, then every literal hoisted, is the stable stage, and the one built against `lib/` formatted -- a hundred and four of its hundred and twelve modules moved -- builds the stable stage (D549); the library's twelve thousand locals and parameters renamed is the stable stage too (D550), which found the index making parameter symbols of a function type's entries; the compilers built against the library with its twelve hundred struct fields reversed and its thirty-six hundred declarations reordered build the stable stage (D551), the foreign modules and `e.mem` keeping their layouts for the other side of an `extern` and for the embedded runtime; and every turn holds in release as well (D533), against a release self-build the suites now build twice byte for byte. Cancellation reaches inside a function (D540), every statement a tick. A compiler-wide structured turn reverses a conflict-free set of 1,571 parameter lists and reproduces the stable stage in debug and release (D562)"}
{"id":"C049","category":"compiler","group":"Front end and language","order":49,"title":"Compiled LLM cards (v1 precursor for T2 H28)","score":1.0,"evidence":"`docs/llm-neper-card.md` is the current alias of the render of `scripts/render_card.py`: the grammar's own productions, its keyword set, the diagnostic registry's families and the stream versions, around the prose of `llm-neper-card.src.md`, stamped with the grammar revision and a content hash, and both suites refuse a card that is not the render (D374). per-symbol API records are `context-file --module` (D397), the card's examples are checked by both suites (D404), and its tool section names every semantic command, plan and the edit loop they make (D425). every diagnostic code the card lists carries its standing -- `verified` by a golden or a suite, `present` in the compiler's source alone, `planned` in the registry alone -- read from the repository at render time (D440); the sixteen documented commands use the same evidence rule (D566), and every displayed grammar production is marked `present` without claiming a fixture map that does not exist (D567). The same render is archived at `docs/llm-neper-card-0.1.md`; the current alias and language-qualified card are both checked, and a future language version writes a new qualified path without overwriting the old one (D569)"}
{"id":"C054","category":"compiler","group":"Front end and language","order":54,"title":"Error detail and partial failure (M2.5-core H07)","score":1.0,"evidence":"Closed by D659. D360 preserves a primary failure across cleanup; D417/D443, D479 and D480 carry caller-owned provenance through host, filesystem and process paths; D614 makes concurrent byte I/O independent; D655-D658 preserve generic stream progress, nested provenance and both allocation-failure steps. Existing compact callbacks remain compatible through additive `DetailReader`/`DetailWriter` types. The normative behavior, measurements and remaining legacy boundary are recorded in `m25-h07-error-detail.md`"}
{"id":"C055","category":"compiler","group":"Front end and language","order":55,"title":"Protocols, generics and compile-time context (M2.5-core H06)","score":1.0,"evidence":"Closed by D730. `explain-file --json` reports every dispatch, failed candidate, generic argument, selected requirement, requesting site, compile-time phase and target layout (D359, D430, D463, D667, D670). Comptime calls, recursion, specializations and diagnostics have explicit structured budgets (D218, D344, D426), while every instance reports NIR and machine-byte cost (D453). Exact function-typed parameters provide explicit strategy selection and preserve operation identity through dependent generic forwarding (D668-D669). Spec section 9 now states equality/hash coherence; exhaustive supplied and declared property fixtures pin reflexivity, symmetry, transitivity and equal-hash agreement, including a harness that detects an intentionally incoherent declaration (D726-D727). Equivalent implicit and explicit strategies agree across a module while a reverse strategy remains distinct (D728). A recursive strategy chain reaches its finite comptime base and is accepted at exactly 66 instances but refused at 65 with E-COMPTIME-0001 (D729). Reproducible five-run Windows and Linux reports over 256 equivalent nominal types find identical instance bytes and image sizes, with no measured explicit-strategy check-time penalty (D730, `benchmarks/protocol_strategies`)"}
{"id":"C056","category":"compiler","group":"Front end and language","order":56,"title":"By-value ABI and alias semantics (M2.5-core H05)","score":1.0,"evidence":"Closed by D654. D358 defines snapshots and reports copy/elide statistics; D650 covers generic aggregates, D651 hidden multiple-return storage, D652 foreign C writes, and D653 cross-module calls in debug plus release inlining. Format 11 prevents snapshot-semantics objects from linking with the former format-10 ABI. The closure record is `m25-h05-value-semantics.md`; callee-effect proof remains an optional optimization, not a correctness gap"}
{"id":"C061","category":"compiler","group":"Front end and language","order":61,"title":"e.meta reflection","score":1,"evidence":"link/meta_scalar answers kind, array_len and type_name; link/meta_reflect walks fields and members through comptime Field and Member values, an unrolled for and get/set at the named offset; link/meta_types answers element_type and backing_type, whose result is a type usable as a comptime argument, nested in the other question, or instantiating a generic. link/meta_field_param hands a comptime Field across a call, so the callee is instantiated per field with a return type that depends on which one it got. A type-valued answer stands where a type is written, including a local's annotation: the type grammar takes a trailing call and name resolution classifies it (D126). A comptime `Field` parameterises a function or a type, and link/meta_field_param pins each instantiation's layout to the field it was built around (D127). link/meta_generic asks the same questions from inside a generic function, where the subject is a type parameter: the template body is checked once with nothing bound, so the question stands there and the instance settles it, which is the deferral `mem.size_of` always had (D136); a `let` initialised by such a protocol call follows its declared type the same way (D143). A call whose callee is a binding's `.ty` is a conversion, the same one a written `u8(x)` is, which is how a walk over `meta.fields` puts a parsed value into the field it belongs to -- every parser answers in one width and every field has its own (D139). A bare type parameter is a conversion the same way, `T(bits)`, and its operand may itself be of a type parameter's type; a `bitcast` to `T` stands in the template pass and is checked per instance (D144)"}
{"id":"C063","category":"compiler","group":"Front end and language","order":63,"title":"Root arena sized by --arena","score":1,"evidence":"`emit-executable --arena SIZE` (D225): the size, with k/m/g, is patched into the image -- a word in the PE runtime the entry reads, the four immediates of the ELF startup stub -- so a self-hosted compiler built with `--arena 1g` compiles the compiler in release, which the default 512 MiB does not hold. link/arena_size pins eight mebibytes refusing twelve on both platforms"}
{"id":"C064","category":"compiler","group":"Front end and language","order":64,"title":"Debug fills 0xCD / 0xDD","score":1,"evidence":"Section 11's two fills (D217): a debug build calls `neper_mem_alloc_fill` and `neper_mem_reset_fill`, which fill what an allocation hands out with 0xCD and what a reset gives back with 0xDD; a release build calls the plain entry points and `@nocheck` changes nothing, since the fills are not checks. link/debug_fills pins both fills in debug and their absence in release, on both platforms"}
{"id":"C067","category":"compiler","group":"Front end and language","order":67,"title":"@reorder opt-in struct packing","score":1,"evidence":"D239 built (D746): `@reorder` above a `type ... = struct`/`union` makes `layout` place that struct's fields one alignment class at a time, highest first, declaration order inside a class -- one deterministic function of the type. The declaration-order C default is untouched, and the fields are still stored and reflected as written. `@reorder` on an enum or tagged union, and a reordered aggregate reaching an `extern`/`@import`/`@cc` signature, are refused as E-TYPE-9999; the crossing walk is behind one checker flag, so a program with no `@reorder` pays nothing. `@packed`, `@align(N)` and `@gpu` do not exist yet, so D239's exclusions against them have nothing to refuse. Pinned on both hosts by link/reorder -- 16 bytes where declaration order pads the same three fields to 24, every field read and written through its own offset, one reordered struct nested in another -- and check/reorder_extern"}
{"id":"C068","category":"compiler","group":"Back end","order":68,"title":"NIR, typed SSA-lite","score":1,"evidence":"src/nir.e"}
{"id":"C069","category":"compiler","group":"Back end","order":69,"title":"Linear-scan register allocator","score":1,"evidence":"src/regalloc.e; a call saves and restores only the registers whose values are live across it, from the allocator's ranges (D226): the compiler's own image is seven per cent smaller. The pool is ten registers (D235): five the calls clobber and five the callee keeps, saved once at entry and restored at the returns, so a value in one of the second five costs nothing at a call. A scalar `var` whose address is used for nothing but loading and storing it is a value with one range and one register, defined again at every store (D236), a loop's own temporaries no longer share one live range with everything the loop touches, and a fixed-register sequence saves only the registers it clobbers that hold a live value. Measured on `e.fmt.json` over the standard files, best of three interleaved against D235: eleven to ninety-nine per cent faster; a byte-counting loop 2.4 times. A comparison whose only use is the branch after it sets the flags and the branch jumps on them, with no materialised bool, and a branch to the next block in code order is dropped (D237); an integer constned under 2^32 is a five-byte load. Measured on `e.fmt.json` over the standard files, best of three interleaved against D235: forty-eight to a hundred and twenty-eight per cent faster, a byte loop 3.3 times. Not in a register still: an aggregate's fields, and a load whose use lies in another block"}
{"id":"C070","category":"compiler","group":"Back end","order":70,"title":"x64 emitter, System V and Win64","score":1,"evidence":"src/codegen_x64.e"}
{"id":"C071","category":"compiler","group":"Back end","order":71,"title":"ELF object writer","score":1,"evidence":"src/object_elf.e"}
{"id":"C072","category":"compiler","group":"Back end","order":72,"title":"COFF object writer","score":1,"evidence":"src/object_coff.e"}
{"id":"C073","category":"compiler","group":"Back end","order":73,"title":"Own ELF linker","score":1,"evidence":"src/link_elf.e"}
{"id":"C074","category":"compiler","group":"Back end","order":74,"title":"Own PE linker with kernel32 imports","score":1,"evidence":"src/link_pe.e"}
{"id":"C075","category":"compiler","group":"Back end","order":75,"title":"Host runtime intrinsics, both platforms","score":1,"evidence":"runtime_pe_x64.asm, runtime_elf_x64.s"}
{"id":"C076","category":"compiler","group":"Back end","order":76,"title":"os.syscall and mem.address_of: the raw kernel path","score":1,"evidence":"link/os_syscall carries a path and a stat buffer through getcwd and newfstatat; link/mem_address pins the address itself and D96 keeps it one-way. It is what lib/e/os.linux.e is written over -- six filesystem primitives in neper rather than asm. Linux only by design; the name does not resolve on Windows"}
{"id":"C077","category":"compiler","group":"Back end","order":77,"title":"`.em` module format with Deps edges","score":1,"evidence":"src/em.e, fixtures/em"}
{"id":"C078","category":"compiler","group":"Back end","order":78,"title":"Dead-function elimination","score":1,"evidence":"only what `main` reaches is emitted, following calls and taken addresses. Both link paths drop the same functions from the same sequence, so an image linked from `.em` artifacts stays byte-identical to one compiled from source -- which link/function_values compares by hash. A minimal e.os binary went from 221 KB to 8 KB and is freestanding again; the compiler's own stage 2 from 4,170,240 to 3,930,624 bytes (D130). Both runtimes are ordered so a function calls only what precedes it and are cut after the last one the program reaches, and the ELF code segment starts where the headers end: and the PE import table declares only what the kept runtime reaches: an empty program is 366 bytes on Linux and 2,048 on Windows, hello world 3,984 and 6,144 (D149, D150, D151)"}
{"id":"C081","category":"compiler","group":"Back end","order":81,"title":"os.thread_create / join / detach","score":1,"evidence":"link/os_thread runs and joins a real thread on both platforms: CreateThread on Windows, clone(2) over a self-allocated stack with a futex join on Linux. detach leaks its mapping, wanting a reaper"}
{"id":"C085","category":"compiler","group":"Self-hosting","order":85,"title":"Self-hosted compiler emits its own source","score":1,"evidence":"run.ps1:623, run.sh:760"}
{"id":"C086","category":"compiler","group":"Self-hosting","order":86,"title":"Stage 2 == stage 3, byte-identical, both platforms","score":1,"evidence":"SHA-256 compare / cmp"}
{"id":"C087","category":"compiler","group":"Self-hosting","order":87,"title":"Bootstrap recovery path archived","score":1,"evidence":"docs/bootstrap-archive.md (D208): the SHA-256 of stages one to four on both platforms at the archived revision, stage three the stable one, and the commands that reproduce them from a clean checkout of the tag `bootstrap-archive-1` with no neper binary present"}
{"id":"T017","category":"tooling","group":"Tooling","order":17,"title":"Module-plan validation in CI","score":1,"evidence":"check_module_plan.py 150 modules; check_module_surfaces.py 28 compiler-resolved sources"}
{"id":"T018","category":"tooling","group":"Tooling","order":18,"title":"Build statistics (`--stats`)","score":1,"evidence":"`emit-executable --stats` and `run --stats` (D308): the program by files, lines, functions, types and attributes; the build by mode, host, target, reached and unreached modules, image size, wall time and every phase; the run by its time, exit code and peak working set (D311); `--stats-full` adds every pool's capacity beside its use. The link's drop count (D334): reached and unreached functions and the code bytes dropped, from the linker's own reachability walk at no cost to the build"}
{"id":"T019","category":"tooling","group":"Tooling","order":19,"title":"`--stats` as a stream record","score":1,"evidence":"With `--json`, `--stats` is one flat `stats` record on stdout before the result (D476): one snake_case key per table row with the unit as its suffix, scalars only, phases as `phase_<name>_ms`, `--stats-full`'s pools as `pool_<name>_capacity` and `_used`, rendered by the same row helpers as the stderr table; `build --json --stats` printed nothing before. Schema entry; both suites validate the record"}
{"id":"T020","category":"tooling","group":"Tooling","order":20,"title":"Image digest reused on a warm build","score":1,"evidence":"D332: the executable's SHA-256 was the one hash a warm build still ran, sixty milliseconds of the compiler's 189. Before the write, `source.matches` compares the packed image with the file at the output path -- the size from a seek to its end first, the way `load` sizes its buffer, and only then the bytes a chunk at a time, stopping at the first that differs -- and, equal, `save_executable` is not called, so the file's mtime stays and nothing watching the binary wakes. `tool.manifest_previous_digest` then takes the artifact's `sha256` from the existing `.neper/<mode>/build-manifest.json` when its one artifact is this path, by the key scan the manifest's other readers use (D255); a manifest missing, unreadable or naming another artifact hashes as before, as does any image that differs. The answer rides `report.build.image_unchanged` rather than a local: `dispatch` is at the bootstrap's 256-local ceiling. Pinned beside the reproducible-build check (D261) in both suites: a third build of the same source into the same output leaves the mtime untouched and its manifest carries the image's digest"}
{"id":"T024","category":"tooling","group":"Tooling","order":24,"title":"Self-host regression suite, both platforms","score":1,"evidence":"305 fixtures, run.ps1 + run.sh"}
{"id":"T025","category":"tooling","group":"Tooling","order":25,"title":"Bootstrap and self-host build scripts, both platforms","score":1,"evidence":"scripts/build-*"}
{"id": "C029", "category": "compiler", "group": "Front end and language", "order": 29, "title": "Vec[T,N] / Mask[T,N] and SIMD lowering", "score": 1, "evidence": "`Vec[T, N]` and `Mask[T, N]` are builtins seeded into e.simd with section 4's closed table, width-aligned layout and `meta.kind/element_type/array_len` answers; every `simd.*` intrinsic but `shuffle` is library source lowered lane by lane over scalar code (D148); section 4's operator table -- IEEE `+ - * /` on float lanes, only `+% -% *%`, the bitwise ones and a scalar shift on integer lanes, `& | ^ ~` on masks -- is checked and lowered lane by lane, plain `+` on integer lanes and every comparison refused (D158); `Vec[T, N]{ ... }` is N positional lanes and `v[i]` reads or writes one, bounds-checked (D159); `shuffle`'s `IDX: [N]u8` is a comptime array parameter, bound from a literal of literals and forwardable by name (D160). a `Mask[T, N]` is register-only (D750): a field, an array element, a slice element, a pointee -- `*Mask` or `&m` -- a module-scope `var`, `mem.alloc`'s element type and `mem.size_of`/`mem.align_of` are each refused under one diagnostic kind naming the position and pointing at `simd.bits(m)`, with a check fixture per position in both suites, but for the written `[]Mask`, which shares `mem.alloc`'s sentence, and the module-scope `var`, which the builtins' seeding order already refuses before the guard is reached. a sixteen-byte lane-wise binary operator lowers to one SSE2 instruction instead of a lane loop (D751): IEEE `+ - * /` on `f32`/`f64` lanes, `+% -%` on every integer width, `*%` on sixteen-bit lanes and `& | ^` on all of them, with section 11's canonical NaN produced lane by lane without a constant pool, checked bit for bit by the `link/simd_lanes` fixture in both suites. `~` on a sixteen-byte integer vector is packed too (D752), as the two instructions no unit has one for: `pcmpeqd` against itself for the all-ones operand and `pxor` against that, carried as `VectorBinary` with the one operand given as both, with a sixteen-lane fixture check whose lanes are all distinct. a sixteen-lane mask's `& | ^` are packed too (D753): its lanes are sixteen bytes of `0` and `1`, so the byte lanes' `pand`, `por` and `pxor` answer them bit for bit with nothing new in the back end, and the sixteen lanes belong to `Mask[i16, 16]` and `Mask[i32, 16]` as well as to a byte vector's mask, all checked in the `link/simd_lanes` fixture over two patterns that differ in every nibble. `~` on a sixteen-lane mask is packed too (D754), and is the one operator that cannot reuse the byte vector's form: a mask lane is a byte of `0` or `1`, so `pxor` against all ones leaves `0xfe` where `0` is wanted and the answer is `pcmpeqb` against zero -- all ones in exactly the lanes that were `0` -- subtracted from that zero, which is the `1` those lanes want. It rides on operation rank 11 with the one operand given as both, loads into `xmm1` so the subtraction is the table's own `psubb` into the register the store reads, and `pcmpeqb` is the only new opcode, with the fixture checking both patterns and that a flipped lane still reduces as a mask lane. an eight-lane mask is packed too (D755): a mask is a byte per lane, so eight of them are the register's low half, which every instruction in the table already operates on and `movlps` -- a quadword in both directions -- loads and stores without reading past the slot; the back end multiplies the immediate's lane code by its lane count for the width, and the closed table keeps a `Vec` from ever being eight bytes, so `Mask[i16, 8]`, `Mask[f32, 8]` and `Mask[u64, 8]` are packed while the thirty-two and sixty-four byte vectors they come from are not, checked over two alternating patterns in the `link/simd_lanes` fixture in both suites. a four-lane mask is packed too (D756): four lanes are four bytes, which `movd` -- the first of these moves to need a mandatory prefix -- carries in both directions, so the same four instructions answer `Mask[i32, 4]`, a sixteen-byte vector's mask, and `Mask[f64, 4]`, a thirty-two-byte vector's, checked over two patterns that agree in two lanes and differ in the other two in the `link/simd_lanes` fixture in both suites. a two-lane mask is packed too (D757), and is the one width the vector unit cannot carry to and from memory on this baseline -- `pinsrw` reads a word but SSE2's `pextrw` only writes one into a general register -- so both directions go through r11, the scratch no value is allocated to: a zero-extending word load and the scalar float path's own `movq` in, that `movq` and a word store out, with the packed operation between them unchanged, which makes `Mask[i64, 2]` and `Mask[f64, 2]` packed and leaves only the masks of more than sixteen lanes on the loop, checked over two patterns that agree in the high lane and differ in the low one in the `link/simd_lanes` fixture in both suites. the masks of more than sixteen lanes are packed too (D758), and want no second register: a packed operation reads no lane it is not given, so an operand wider than one is the same instruction once per sixteen-byte chunk over the chunk's own addresses, which lowering names with the `FieldAddress` its lane loop already uses and the back end never sees -- every instruction it is given is still sixteen bytes -- which packs `Mask[u8, 32]`, `Mask[i16, 32]` and `Mask[u8, 64]` and leaves every mask the closed table has packed, checked over patterns that differ in every chunk in the `link/simd_lanes` fixture in both suites. the thirty-two and sixty-four byte vectors are packed too (D759), which is the chunk loop's condition and nothing else: the wide widths are admitted for every lane type and not for a mask's byte lanes only, so a `Vec[f32, 8]` is two instructions and a `Vec[u8, 64]` four, each canonicalising its own NaN lanes and each loaded by the unaligned move a chunk at offset 16 needs, checked in the `link/simd_lanes` fixture over lanes written in every chunk in both suites. a lane-wise shift by a constant count is packed too (D760), and is the first packed form whose right operand is not a vector: the baseline carries the count inside the instruction -- `psllw`/`psrlw`/`psraw`, their thirty-two-bit three and `psllq`/`psrlq`, with the modrm register field saying which -- so lowering takes the form only when the count folded to a `ConstInteger` it can read and is below the lane's width, which leaves every other count on the lane loop and with it the check that traps on a count the width does not admit. The count rides above the immediate's three fields, where everything written before it is zero, and passes through the chunk loop unchanged, so a thirty-two-byte vector is two instructions with the one count. a shift by a count in a register is packed too (D761), which is the shape D760 left on the lane loop and the baseline does have an instruction for: the count rides in the low quadword of a second register instead of a byte of the encoding, flagged by a bit above the immediate's count field, and the third operand the constant form already named keeps it live for the allocator. Section 11's two halves run in order -- a `cmp` against the width guards a trap naming the count and the width in debug, an `and r10, width - 1` masks in every mode -- through `r10` and `r11`, the registers the allocator never hands out, so the check needs no spill and cannot reuse the scalar path's helper, which shifts through `rcx`. A count the width does not admit is no longer a constant either: it takes this form and traps. The signed sixty-four-bit right shift stays on the lane loop, as there is no `psraq`, and a wide vector reloads the count per chunk, since the back end sees one chunk at a time. Checked in both suites by the `link/simd_lanes` fixture, over all three widths in both directions and across a thirty-two-byte vector's two chunks, and by `link/trap_arithmetic`'s new `vshift` mode, which reads the vector trap's own line, column and message. `*%` on thirty-two-bit lanes is packed too (D762), out of the instruction the baseline does have: `pmuludq` multiplies the low half of each sixty-four-bit lane into the whole lane, and `*%` wants a lane's low thirty-two bits, which are the same bits whichever width the product was taken at -- so two of them, one on the operands as given and one on each shuffled by `0xb1`, cover all four lanes, `pshufd` by `0x08` gathers the low doubleword of each pair of products and `punpckldq` puts the four in order. Seven instructions, nothing new in the allocator, the same selection for `i32` and `u32` since a low half has no sign, and once per chunk for a wider vector, checked in the `link/simd_lanes` fixture over four distinct lanes that each need a high half dropped and eight lanes with one written in each chunk, in both suites. Sixty-four-bit lanes take the third `pmuludq` form (D763): the low halves' product plus both cross products shifted up thirty-two, ten instructions and nothing new in the allocator, checked in the same fixture over `i64` lanes whose product crosses 2^64 and `u64` lanes across two chunks, in debug and release on both hosts. Byte lanes take a `pmullw` form (D764): the bytes unpacked against themselves, since 257 is 1 modulo 256, multiplied as words, masked and packed back, fourteen instructions and nothing new in the allocator, checked over sixteen distinct `u8` lanes, sixteen `i8` lanes and two chunks of `u8`, in debug and release on both hosts; every integer width of `*%` is packed now, and `f16` keeps the lane loop by design, its arithmetic being F16C or AVX-512. `--cpu x64-v3` (D765) lowers every thirty-two-byte vector operation as one VEX.256 instruction over a ymm register, the same selection through the emitter's VEX prefix, the level in the build identity and the manifest, `info` listing the levels, the fixture run at the level in both modes on both hosts. Section 5's xmm calling convention is deferred to the vector register class under C044 (D786): on this back end a vector crosses a neper call as copied storage, which no program can observe"}
