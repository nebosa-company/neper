// A generated file edited by hand (D418, H19): the map's generator input is
// unchanged, the generated file is not what the map recorded, so nothing
// regenerated it -- E-TOOL-0002, not a stale map.
use e.mem

fn main(a: *mem.Arena, args: []str) -> err {
    let n: i32 = 2
    ret ok
}
