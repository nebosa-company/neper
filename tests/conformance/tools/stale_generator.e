// A version 2 map whose generator input changed since the file was generated (D373):
// E-TOOL-0001 and no artifact, though the generated file's own hash still matches.
use e.mem

fn main(a: *mem.Arena, args: []str) -> err {
    ret ok
}
