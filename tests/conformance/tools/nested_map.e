// A nested map (D465, H19): this file is generated from `nested_map.mid`, which is
// itself generated from `nested_map.input` under a map of its own; the diagnostic lands
// on the root original, with the intermediate and the generated spans related.
use e.mem

fn main(a: *mem.Arena, args: []str) -> err {
    let n: i32 = true
    ret ok
}
