// A chain of maps (D499, D564, H19): this file is generated through mid1, mid2,
// mid3 and mid4 from `nested_deep.input`; the diagnostic lands on the root, with
// all four intermediates and the generated span related, root-nearest first.
use e.mem

fn main(a: *mem.Arena, args: []str) -> err {
    let n: i32 = true
    ret ok
}
