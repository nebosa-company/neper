// A chain of maps (D499, H19): this file is generated from `nested_deep.mid1`, itself
// from `nested_deep.mid2`, itself from `nested_deep.input`; the diagnostic lands on
// the root, with both intermediates and the generated span related, root-nearest first.
use e.mem

fn main(a: *mem.Arena, args: []str) -> err {
    let n: i32 = true
    ret ok
}
