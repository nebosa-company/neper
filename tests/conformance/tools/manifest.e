// `build-manifest --json` (D236): the canonical neper-build-manifest object -- versions,
// target, mode, root module, and one input per source module with its SHA-256.
const LIMIT: i32 = 100i32
fn scale(x: i32) -> i32 {
    ret x *% 2i32
}
