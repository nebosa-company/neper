// A generated file under a version 2 source map (D373, H19): the generator and its
// input are named, the input's hash is checked, and the one mapping is
// regeneration-owned, so the diagnostic's related location says to edit the original.
use e.mem

fn main(a: *mem.Arena, args: []str) -> err {
    let n: i32 = true
    ret ok
}
