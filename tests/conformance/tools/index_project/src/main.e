// `index` over a project (D298): every module under src in byte order, each under its
// path from src, the records under one header and one result.
use e.mem
use util

fn main(a: *mem.Arena, args: []str) -> err {
    let n = util.twice(2i32)
    ret ok
}
