error Failed

const LIMIT: usize = 4usize

type Id = i64

type Pair = struct {
    left: i32,
    right: i32,
}

type Choice = enum u8 {
    None,
    Some = 3u8,
}

fn main() -> i64 {
    ret 0i64
}
