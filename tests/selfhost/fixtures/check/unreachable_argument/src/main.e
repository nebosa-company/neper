// `unreachable` takes a `str` literal or nothing: its argument is the trap record's
// text, which the back end lays out beside the site, so a value is refused.
fn main() -> err {
    let code = 3i32
    unreachable(code)
    ret ok
}
